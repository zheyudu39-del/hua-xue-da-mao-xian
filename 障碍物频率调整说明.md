# 障碍物频率调整说明

## 功能概述
降低小屋和石头的出现频率，使游戏更加轻松和舒适，减少障碍物密度。

## 调整内容

### 1. 生成间隔调整 ⏱️

#### 修改前
```javascript
obstacleSpawnInterval: 3500  // 3.5秒生成一次
```

#### 修改后
```javascript
obstacleSpawnInterval: 6000  // 6秒生成一次
```

**效果**：
- 生成间隔：3.5秒 → 6秒
- 增加：+2.5秒（+71%）
- 频率降低：约 **42%**

### 2. 生成概率调整 🎲

#### 新增概率控制
```javascript
spawnObstacle() {
    // 降低生成概率：60%的机会生成障碍物
    if (Math.random() > 0.6) {
        return; // 40%的时间不生成任何障碍物
    }
    
    const type = Math.random() > 0.5 ? 'rock' : 'house';
    // ... 生成逻辑
}
```

**效果**：
- 生成概率：100% → 60%
- 降低：40%
- 40%的时间不生成任何障碍物

### 3. 综合效果 📊

#### 实际出现频率计算
```
原频率 = 1次 / 3.5秒 × 100% = 0.286次/秒
新频率 = 1次 / 6秒 × 60% = 0.1次/秒

频率降低 = (0.286 - 0.1) / 0.286 × 100% = 65%
```

**综合降低约 65%！**

## 频率对比

### 时间线对比

#### 修改前（30秒内）
```
0秒  3.5秒  7秒  10.5秒  14秒  17.5秒  21秒  24.5秒  28秒
 |     |     |     |      |      |      |      |      |
 🪨    🏠    🪨    🏠     🪨     🏠     🪨     🏠     🪨

30秒内约8-9个障碍物
```

#### 修改后（30秒内）
```
0秒      6秒      12秒      18秒      24秒      30秒
 |        |        |         |         |         |
 X       🪨        X        🏠        X         X

30秒内约2-3个障碍物（考虑60%概率）
```

### 数量对比表

| 时间段 | 修改前 | 修改后 | 减少 |
|--------|--------|--------|------|
| 10秒 | 2-3个 | 0-1个 | -67% |
| 30秒 | 8-9个 | 2-3个 | -70% |
| 60秒 | 17-18个 | 6个 | -65% |
| 120秒 | 34-35个 | 12个 | -65% |

## 游戏体验变化

### 障碍物密度

#### 修改前
```
密集：
🪨 🏠 🪨 🏠 🪨 🏠 🪨 🏠
   ↑ 平均3.5秒一个
```

#### 修改后
```
稀疏：
🪨    X    X    🏠    X    X
   ↑ 平均10秒一个（考虑概率）
```

### 难度变化

| 指标 | 修改前 | 修改后 | 变化 |
|------|--------|--------|------|
| 障碍物密度 | 高 | 低 | -65% |
| 躲避难度 | 困难 | 简单 | -65% |
| 碰撞风险 | 高 | 低 | -65% |
| 游戏节奏 | 紧张 | 轻松 | 更舒适 |
| 观赏性 | 中 | 高 | 更多空间欣赏场景 |

### 玩家体验

#### 修改前
- ⚠️ 障碍物频繁
- ⚠️ 需要持续集中注意力
- ⚠️ 容易频繁碰撞
- ⚠️ 压力较大

#### 修改后
- ✅ 障碍物稀疏
- ✅ 有更多喘息时间
- ✅ 碰撞次数减少
- ✅ 游戏更轻松
- ✅ 更多时间欣赏场景
- ✅ 更适合休闲玩家

## 技术细节

### 生成间隔控制
```javascript
// 在update方法中
if (Date.now() - this.lastObstacleSpawn > CONFIG.obstacleSpawnInterval) {
    this.spawnObstacle();
    this.lastObstacleSpawn = Date.now();
}
```

**工作原理**：
1. 检查距离上次生成的时间
2. 如果超过6秒（6000ms）
3. 调用生成函数
4. 记录当前时间

### 概率控制
```javascript
if (Math.random() > 0.6) {
    return; // 40%的时间不生成
}
```

**工作原理**：
1. 生成0-1之间的随机数
2. 如果大于0.6（40%概率）
3. 直接返回，不生成障碍物
4. 否则继续生成（60%概率）

### 障碍物类型
```javascript
const type = Math.random() > 0.5 ? 'rock' : 'house';
```

**分布**：
- 石头：50%
- 小屋：50%

**实际出现概率**（考虑60%生成率）：
- 石头：30%（60% × 50%）
- 小屋：30%（60% × 50%）
- 不生成：40%

## 配置参数

### CONFIG对象
```javascript
const CONFIG = {
    // ... 其他配置
    obstacleSpawnInterval: 6000  // 障碍物生成间隔（毫秒）
};
```

**可调整范围**：
- 最小值：3000ms（3秒）- 非常频繁
- 推荐值：6000ms（6秒）- 适中
- 最大值：10000ms（10秒）- 非常稀疏

### 概率参数
```javascript
if (Math.random() > 0.6) {  // 可调整这个值
    return;
}
```

**可调整范围**：
- 0.3：70%生成率 - 较频繁
- 0.5：50%生成率 - 适中
- **0.6：60%生成率 - 当前设置**
- 0.7：30%生成率 - 稀疏
- 0.8：20%生成率 - 非常稀疏

## 实际效果示例

### 场景1：10秒游戏时间

#### 修改前
```
时间轴：
0s   3.5s   7s   10s
|     |     |     |
🪨    🏠    🪨   (下一个即将出现)

结果：3个障碍物
```

#### 修改后
```
时间轴：
0s        6s        10s
|         |         |
X        🪨        (还要等2秒)

结果：1个障碍物（考虑概率）
```

### 场景2：60秒游戏时间

#### 修改前
```
障碍物数量：17-18个
平均间隔：3.5秒
密度：高
```

#### 修改后
```
障碍物数量：6个
平均间隔：10秒
密度：低
```

## 代码位置

### 配置文件（game.js 第17行）
```javascript
obstacleSpawnInterval: 6000
```

### 生成函数（game.js 第412-423行）
```javascript
spawnObstacle() {
    // 降低生成概率：60%的机会生成障碍物
    if (Math.random() > 0.6) {
        return; // 40%的时间不生成任何障碍物
    }
    
    const type = Math.random() > 0.5 ? 'rock' : 'house';
    const groundY = this.terrain.getHeightAt(this.canvas.width);
    const height = type === 'rock' ? 90 : 216;
    const y = groundY - height;
    this.obstacles.push(new Obstacle(this.canvas.width, y, type, this.terrain));
}
```

### 调用位置（game.js 第205-208行）
```javascript
if (Date.now() - this.lastObstacleSpawn > CONFIG.obstacleSpawnInterval) {
    this.spawnObstacle();
    this.lastObstacleSpawn = Date.now();
}
```

## 进一步调整建议

### 如果还想更稀疏
```javascript
// 方案1：增加间隔到8秒
obstacleSpawnInterval: 8000

// 方案2：降低概率到50%
if (Math.random() > 0.5) {
    return;
}

// 方案3：两者结合
obstacleSpawnInterval: 8000
if (Math.random() > 0.5) {
    return;
}
// 这样频率会降低约75%
```

### 如果想分别控制小屋和石头
```javascript
spawnObstacle() {
    // 石头：30%概率
    // 小屋：20%概率
    // 不生成：50%概率
    
    const rand = Math.random();
    
    if (rand < 0.3) {
        type = 'rock';
    } else if (rand < 0.5) {
        type = 'house';
    } else {
        return; // 不生成
    }
    
    // ... 生成逻辑
}
```

## 总结

### ✅ 实现效果
- ⏱️ **生成间隔**：3.5秒 → 6秒（+71%）
- 🎲 **生成概率**：100% → 60%（-40%）
- 📊 **综合频率**：降低约 **65%**
- 🎮 **游戏体验**：更轻松、更舒适

### 🎯 频率对比
| 时间 | 修改前 | 修改后 | 减少 |
|------|--------|--------|------|
| 30秒 | 8-9个 | 2-3个 | -70% |
| 60秒 | 17-18个 | 6个 | -65% |

### 📈 体验提升
- 障碍物密度：⭐⭐⭐⭐⭐ → ⭐⭐
- 游戏难度：⭐⭐⭐⭐⭐ → ⭐⭐⭐
- 轻松程度：⭐⭐ → ⭐⭐⭐⭐⭐
- 观赏性：⭐⭐⭐ → ⭐⭐⭐⭐⭐

---

**版本**：障碍物频率调整 v1.0
**更新日期**：2025-11-21
**状态**：✅ 已完成

刷新浏览器体验更轻松的游戏节奏！🎮✨
